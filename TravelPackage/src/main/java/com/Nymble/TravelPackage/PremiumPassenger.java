package com.Nymble.TravelPackage;

class PremiumPassenger extends Passenger {
    public PremiumPassenger(String name, int number, double balance) {
        super(name, number, balance);
    }
}